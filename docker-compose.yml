version: '3.8'

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: classroom-frontend-app
    restart: always
    ports:
      - "5173:80"
    environment:
      - VITE_API_URL=http://192.168.66.133
