server {
    listen 80;
    server_name localhost;

    resolver 127.0.0.11 valid=30s;

    # Định tuyến cho Frontend (Single Page Application)
    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        try_files $uri $uri/ /index.html;
    }

    # Định tuyến cho User Service (Cổng 8081)
    location /api/ {
        set $user_backend http://user-service-app:8081; 
        proxy_pass $user_backend; 
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    # Định tuyến cho Class Service (Cổng 8082)
    location /class-api/ {
        set $class_backend http://class-service-app:8082;

        rewrite ^/class-api/(.*)$ /api/$1 break;
        
        proxy_pass $class_backend;
        
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    error_page 500 502 503 504 /50x.html;
    location = /50x.html {
        root /usr/share/nginx/html;
    }
}